const defaultState = {
    singers: [
        {id: 1, name: 'singer1'},
        {id: 2, name: 'singer2'}
    ]
}

const reducer = (state = defaultState) => {
    return state;
};

export default reducer