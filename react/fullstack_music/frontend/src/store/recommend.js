let defaultState = {
    banners: [
        { id: 1, name: 'aaaa' },
        { id: 2, name: 'bbbb' },
    ],
};

const reducer = (state = defaultState) => {
    return state;
};

export default reducer;
